[phases.setup]
nixPkgs = ["nodejs_20"]
cmds = ["export COREPACK_ENABLE_STRICT=0 && corepack enable && corepack prepare pnpm@10.7.1 --activate"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["node ./build.mjs"]

[start]
cmd = "node --enable-source-maps ./dist/index.mjs"
