kind = "web"
previewPath = "/"
title = "ClipRank"
version = "1.0.0"
id = "artifacts/cliprank"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/cliprank run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cliprank", "run", "build" ]
publicDir = "artifacts/cliprank/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
